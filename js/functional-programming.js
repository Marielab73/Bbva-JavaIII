import { maria, fulanito, fulanita } from "./oop.js";

/* Crear un objeto llamado "functionTests" que tenga las siguientes propiedades y métodos:
  * name (string) con cualquier valor
  * thisInArrow: Imprime el mensaje `Mi dueño es ${this.name}` usando una función flecha
  * thisInRegular: Hace lo mismo que el anterior pero lo declaramos usando una función regular

  ¿Qué pasa cuando llamamos a cada función? ¿Por qué?
*/

/* Implementar propio map, filter y reduce */

/* Usar el array "primes" para realizar las siguientes operaciones:
    * Crear un nuevo array con todos los números que se encuentran índices impares.
    * Del nuevo array multiplicar todos sus elementos por 2.
    * Imprimir por consola la sumatoria del último array.
    
  No deben usar loops en ninguna de las operaciones.
*/

/* 
  Crear una función "createGreeter" que reciba como parámetro un saludo y retorne una función que recibe una persona e imprime el saludo seguido del nombre y un signo de exclamación.
*/

/* Crear las funciones:
  * "bienvenida" que salude cree un saludo usando la función "createGreeter" y el mensaje "Hola [nombre de la persona]!".
  * "adios" que cree un saludo usando la función "createGreeter" y el mensaje "Nos vemos pronto, [nombre de la persona]!".
*/
